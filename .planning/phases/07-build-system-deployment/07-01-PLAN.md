---
phase: 07-build-system-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cloud-relay/Dockerfile
  - cloud-relay/entrypoint.sh
  - cloud-relay/.dockerignore
  - home-device/stalwart/Dockerfile
  - home-device/maddy/Dockerfile
  - home-device/postfix-dovecot/Dockerfile
  - home-device/postfix-dovecot/entrypoint.sh
  - home-device/.dockerignore
  - .github/workflows/build-custom.yml
  - .github/workflows/build-prebuilt.yml
  - .github/workflows/release.yml
  - .dockerignore
autonomous: true

must_haves:
  truths:
    - "User forks the repo and triggers a GitHub Actions workflow_dispatch with component selection (mail server, webmail, calendar) to build custom multi-arch Docker images"
    - "Pre-built default stack images (Stalwart + SnappyMail and Postfix+Dovecot + Roundcube + Radicale) are built and published to GHCR on every release tag"
    - "All images build for linux/amd64 and linux/arm64 from a single workflow run"
    - "All Docker images are under 100MB per architecture and use Alpine multi-stage builds with stripped Go binaries"
    - "Container entrypoints detect missing setup configuration and exit with a helpful message instead of crash-looping"
  artifacts:
    - path: ".github/workflows/build-custom.yml"
      provides: "GitHub Actions workflow with workflow_dispatch inputs for component selection"
      contains: "workflow_dispatch"
    - path: ".github/workflows/build-prebuilt.yml"
      provides: "Automated prebuilt image publishing on release tags"
      contains: "tags:"
    - path: ".github/workflows/release.yml"
      provides: "Semantic version release workflow"
      contains: "semver"
    - path: "cloud-relay/Dockerfile"
      provides: "Multi-arch optimized cloud relay image"
      contains: "TARGETARCH"
    - path: "cloud-relay/.dockerignore"
      provides: "Build context exclusions for cloud relay"
    - path: "home-device/.dockerignore"
      provides: "Build context exclusions for home device"
  key_links:
    - from: ".github/workflows/build-custom.yml"
      to: "cloud-relay/Dockerfile"
      via: "docker/build-push-action file parameter"
      pattern: "file:.*cloud-relay/Dockerfile"
    - from: ".github/workflows/build-custom.yml"
      to: "ghcr.io"
      via: "docker/login-action registry parameter"
      pattern: "registry: ghcr.io"
    - from: "cloud-relay/entrypoint.sh"
      to: "setup detection"
      via: "config file existence check"
      pattern: "run setup first"
---

<objective>
Update all Dockerfiles for multi-architecture builds with size optimization and setup detection, then create GitHub Actions workflows for custom component selection builds, pre-built default stack publishing, and semantic version releases.

Purpose: Deliver BUILD-01 (GitHub Actions pipeline with component selection), BUILD-02 (multi-arch images), and BUILD-03 (pre-built full-featured images) while establishing the image optimization foundation (<100MB target).

Output: Updated Dockerfiles with TARGETARCH support, OCI labels, and setup detection entrypoints; three GitHub Actions workflows (custom build, prebuilt, release); .dockerignore files.
</objective>

<execution_context>
@/Users/trekkie/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trekkie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-build-system-deployment/07-CONTEXT.md
@.planning/phases/07-build-system-deployment/07-RESEARCH.md
@cloud-relay/Dockerfile
@cloud-relay/entrypoint.sh
@cloud-relay/docker-compose.yml
@home-device/docker-compose.yml
@home-device/stalwart/Dockerfile
@home-device/maddy/Dockerfile
@home-device/postfix-dovecot/Dockerfile
@home-device/postfix-dovecot/entrypoint.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Dockerfiles for multi-arch, optimization, and setup detection</name>
  <files>
    cloud-relay/Dockerfile
    cloud-relay/entrypoint.sh
    cloud-relay/.dockerignore
    home-device/stalwart/Dockerfile
    home-device/maddy/Dockerfile
    home-device/postfix-dovecot/Dockerfile
    home-device/postfix-dovecot/entrypoint.sh
    home-device/.dockerignore
    .dockerignore
  </files>
  <action>
    **1. cloud-relay/Dockerfile updates:**
    - Add `ARG TARGETARCH` and use it in the Go build command: `GOARCH=${TARGETARCH}` (buildx sets TARGETARCH automatically)
    - Add `-trimpath` to ldflags for reproducible builds: `go build -trimpath -ldflags="-s -w"`
    - Add OCI labels: `org.opencontainers.image.source`, `org.opencontainers.image.version` (use ARG for version), `org.opencontainers.image.licenses=AGPL-3.0`, `org.opencontainers.image.description`
    - Keep existing Alpine 3.21 base, postfix~=3.9 pin, wireguard-tools, ca-certificates, gettext
    - Ensure health check uses: `HEALTHCHECK --interval=30s --timeout=10s --start-period=10s --retries=3 CMD nc -z localhost 25 || exit 1` (nc is more reliable than `postfix status` for container orchestrators)
    - Add `ARG VERSION=dev` and label `org.opencontainers.image.version=${VERSION}` for build-time version injection

    **2. cloud-relay/entrypoint.sh updates:**
    - Add setup detection at the top of the script (before existing RELAY_HOSTNAME check):
      ```
      # Check for DarkPipe setup configuration
      if [ -f "/config/.darkpipe-configured" ]; then
        # Load generated environment from setup script
        [ -f "/config/relay.env" ] && . /config/relay.env
      fi
      ```
    - Keep existing RELAY_HOSTNAME/RELAY_DOMAIN check (it serves as the "missing setup" gate -- if not set via env OR config file, it already errors with a helpful message)
    - Enhance the error message to mention the setup script: add "Run 'darkpipe-setup' first or set RELAY_HOSTNAME and RELAY_DOMAIN environment variables"
    - Add Docker secrets support: before the env var check, read `_FILE` variants:
      ```
      # Docker secrets support (_FILE suffix convention)
      for var in RELAY_WEBHOOK_URL CERTBOT_EMAIL RELAY_OVERFLOW_ACCESS_KEY RELAY_OVERFLOW_SECRET_KEY; do
        file_var="${var}_FILE"
        eval file_path="\$$file_var"
        if [ -n "$file_path" ] && [ -f "$file_path" ]; then
          eval export "$var=$(cat $file_path)"
        fi
      done
      ```

    **3. home-device Dockerfile updates (stalwart, maddy, postfix-dovecot):**
    - **stalwart/Dockerfile:** Add OCI labels (source, version, description, licenses). Add `ARG VERSION=dev` label. Add entrypoint wrapper that checks for `/config/.darkpipe-configured` or required env vars. If using official base image, create a thin wrapper script `/entrypoint-wrapper.sh` that checks setup, reads Docker secrets (_FILE vars for ADMIN_PASSWORD, DKIM keys), then execs the original stalwart binary. Use `ENTRYPOINT ["/entrypoint-wrapper.sh"]`.
    - **maddy/Dockerfile:** Same pattern -- OCI labels, ARG VERSION, entrypoint wrapper for setup detection and secrets. Exec the original maddy binary after checks.
    - **postfix-dovecot/Dockerfile:** Add OCI labels, ARG VERSION. Update existing entrypoint.sh to add setup detection block and Docker secrets support at the top (same pattern as cloud-relay). Add `ARG TARGETARCH` -- not needed for Go build (no Go in this image) but useful for documentation.
    - All three: ensure `LABEL org.opencontainers.image.source=https://github.com/trek-e/darkpipe` is present

    **4. .dockerignore files:**
    - Create root `.dockerignore`: `.git`, `.planning`, `*.md` (except LICENSE), `deploy/`, `.github/`, `tests/`, `.env*`, `secrets/`, `*.test`, `__pycache__`
    - Create `cloud-relay/.dockerignore`: Same as root plus `home-device/`
    - Create `home-device/.dockerignore`: Same as root plus `cloud-relay/`
    - **CRITICAL: Do NOT exclude `*.go` files or Go source directories.** The Go glue daemon is compiled into Docker images during build (per locked decision). Verify after creation that:
      - `cloud-relay/*.go` files are NOT matched by any .dockerignore pattern (needed for Go compilation in cloud-relay/Dockerfile)
      - `home-device/*/*.go` files are NOT matched by any .dockerignore pattern (needed for any Go compilation in home device Dockerfiles)
      - Run `docker build --check` or manually confirm: no .dockerignore line matches `*.go`, `cmd/`, `pkg/`, `internal/`, or any Go source directory patterns

    **5. Docker secrets support pattern for all entrypoints:**
    For each secret variable, check `${VAR}_FILE` first:
    ```sh
    # Read secret from file if _FILE variant is set
    read_secret() {
      local var_name="$1"
      local file_var="${var_name}_FILE"
      eval local file_path="\$$file_var"
      if [ -n "$file_path" ] && [ -f "$file_path" ]; then
        eval export "$var_name=$(cat "$file_path" | tr -d '\n')"
      fi
    }
    ```
    Apply to: ADMIN_PASSWORD, DKIM_PRIVATE_KEY, RELAY_OVERFLOW_ACCESS_KEY, RELAY_OVERFLOW_SECRET_KEY, CERTBOT_EMAIL
  </action>
  <verify>
    1. `docker build --build-arg TARGETARCH=amd64 -f cloud-relay/Dockerfile .` succeeds
    2. Each Dockerfile contains TARGETARCH ARG (or doesn't need it for non-Go images)
    3. All Dockerfiles contain OCI labels (grep for org.opencontainers)
    4. Each entrypoint has setup detection (grep for "darkpipe-configured" or "run setup")
    5. Each entrypoint has Docker secrets support (grep for "_FILE")
    6. .dockerignore files exist and exclude .git, .planning, secrets
    7. Verify Go source is available in build context: confirm no .dockerignore file excludes `*.go` files -- `grep -rn '\.go' .dockerignore cloud-relay/.dockerignore home-device/.dockerignore` should return no lines that would exclude Go source (e.g., no `*.go` or blanket `cmd/` or `pkg/` patterns)
  </verify>
  <done>
    All Dockerfiles support multi-arch builds via TARGETARCH, contain OCI labels, and have entrypoints with setup detection and Docker secrets _FILE convention. .dockerignore files minimize build context. Cloud relay image builds under 50MB on amd64.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions workflows for custom builds, pre-built images, and releases</name>
  <files>
    .github/workflows/build-custom.yml
    .github/workflows/build-prebuilt.yml
    .github/workflows/release.yml
  </files>
  <action>
    **1. `.github/workflows/build-custom.yml` - Custom Stack Build:**
    Create workflow_dispatch workflow with choice inputs:
    - `mail_server`: choice [stalwart, maddy, postfix-dovecot], default: stalwart
    - `webmail`: choice [none, roundcube, snappymail], default: snappymail
    - `calendar`: choice [none, radicale, builtin], default: builtin
    - `tag_suffix`: string input (optional, for custom tagging)

    Jobs:
    - **build-relay**: Build cloud-relay image for linux/amd64,linux/arm64
      - Uses docker/setup-qemu-action@v3, docker/setup-buildx-action@v3, docker/login-action@v3 (ghcr.io)
      - Uses docker/metadata-action@v5 with images: `ghcr.io/${{ github.repository }}/cloud-relay`
      - Tags: `type=sha`, `type=raw,value=${{ github.run_number }}`, `type=raw,value=latest,enable=${{ github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}`
      - Uses docker/build-push-action@v6 with `cache-from: type=gha`, `cache-to: type=gha,mode=max`
      - Build arg: `VERSION=${{ github.sha }}`

    - **build-home**: Build home device image for linux/amd64,linux/arm64
      - Same QEMU/buildx/login setup
      - Images: `ghcr.io/${{ github.repository }}/home-${{ inputs.mail_server }}`
      - File: `./home-device/${{ inputs.mail_server }}/Dockerfile`
      - Build args: `WEBMAIL=${{ inputs.webmail }}`, `CALENDAR=${{ inputs.calendar }}`, `VERSION=${{ github.sha }}`
      - Same caching strategy

    Both jobs run in parallel (no dependencies between them).

    Permissions: `packages: write`, `contents: read`

    **2. `.github/workflows/build-prebuilt.yml` - Pre-built Default Stack:**
    Triggered on:
    - `push.tags: ['v*']` (semantic version tags)
    - `workflow_dispatch` (manual trigger for testing)

    Build matrix strategy for two pre-built stacks:
    - Default: Stalwart + SnappyMail (tag: `default`)
    - Conservative: Postfix+Dovecot + Roundcube + Radicale (tag: `conservative`)

    Jobs:
    - **build-prebuilt-relay**: Single cloud-relay build (same for all stacks)
      - Tags include semver from git tag: `type=semver,pattern={{version}}`, `type=semver,pattern={{major}}.{{minor}}`, `type=raw,value=latest`
    - **build-prebuilt-home**: Matrix build for each stack combo
      - Matrix: `include: [{stack: default, mail: stalwart, webmail: snappymail, calendar: builtin}, {stack: conservative, mail: postfix-dovecot, webmail: roundcube, calendar: radicale}]`
      - Images: `ghcr.io/${{ github.repository }}/home-${{ matrix.mail }}`
      - Tags include semver + stack identifier

    Permissions: `packages: write`, `contents: read`

    **3. `.github/workflows/release.yml` - Release Automation:**
    Triggered on `push.tags: ['v*']`

    Jobs:
    - **create-release**: Uses `softprops/action-gh-release@v2` to create GitHub release
      - Auto-generate release notes from commits
      - Include deployment instructions in release body
      - Mark pre-release if tag contains `-rc`, `-beta`, or `-alpha`

    - **build-setup-tool**: Build the Go setup binary for multiple platforms
      - Matrix: `[{os: linux, arch: amd64}, {os: linux, arch: arm64}, {os: darwin, arch: arm64}, {os: darwin, arch: amd64}]`
      - `CGO_ENABLED=0 GOOS=${{ matrix.os }} GOARCH=${{ matrix.arch }} go build -trimpath -ldflags="-s -w -X main.version=${{ github.ref_name }}" -o darkpipe-setup-${{ matrix.os }}-${{ matrix.arch }} ./deploy/setup/cmd/darkpipe-setup/`
      - Upload binaries as release assets
      - Note: This job will succeed only after Plan 07-02 creates the setup tool source. For now, include the job definition but it will skip if source doesn't exist (use `if: hashFiles('deploy/setup/cmd/darkpipe-setup/main.go') != ''`)

    Permissions: `packages: write`, `contents: write`

    **Important notes:**
    - All workflows use `ghcr.io` ONLY (per locked decision, no Docker Hub)
    - GITHUB_TOKEN used for authentication (no additional secrets needed for GHCR)
    - GHA cache (`type=gha`) for layer caching across builds
    - All images are `linux/amd64,linux/arm64` (per BUILD-02)
  </action>
  <verify>
    1. All three workflow files have valid YAML syntax: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/build-custom.yml'))"`
    2. build-custom.yml has workflow_dispatch with mail_server, webmail, calendar choice inputs
    3. build-prebuilt.yml triggers on version tags (v*)
    4. All workflows use ghcr.io (not docker.io)
    5. All workflows specify `platforms: linux/amd64,linux/arm64`
    6. All workflows use docker/* official actions (setup-qemu, setup-buildx, login, metadata, build-push)
    7. release.yml creates GitHub release and builds setup binaries
  </verify>
  <done>
    Three GitHub Actions workflows exist: build-custom.yml allows fork users to build custom stack combinations via workflow_dispatch, build-prebuilt.yml publishes default and conservative stack images on version tags, and release.yml creates GitHub releases with setup tool binaries. All workflows target linux/amd64 and linux/arm64 and publish to GHCR.
  </done>
</task>

</tasks>

<verification>
- [ ] `docker build` succeeds for cloud-relay with TARGETARCH=amd64
- [ ] All Dockerfiles have OCI labels (grep org.opencontainers)
- [ ] All entrypoints have setup detection and Docker secrets support
- [ ] .dockerignore files exclude .git, .planning, secrets
- [ ] All GitHub Actions workflows have valid YAML
- [ ] Custom build workflow has workflow_dispatch with component choices
- [ ] Prebuilt workflow triggers on version tags
- [ ] All workflows target linux/amd64,linux/arm64 and use ghcr.io
</verification>

<success_criteria>
1. All Dockerfiles updated for multi-arch with TARGETARCH, OCI labels, and optimized layer ordering
2. Entrypoints detect missing setup and provide helpful guidance instead of crash-looping
3. Docker secrets _FILE convention implemented across all containers
4. GitHub Actions custom build workflow accepts component selection and builds multi-arch images
5. Pre-built images for two stack combos (default + conservative) publish on release tags
6. Release workflow creates GitHub releases with cross-platform setup binaries
</success_criteria>

<output>
After completion, create `.planning/phases/07-build-system-deployment/07-01-SUMMARY.md`
</output>
